# Appwrite — jede App nutzt ihre EIGENE Instanz. Für branding.supply ist das
# das Projekt `branding` (Infra-Plan §3, Session-Cookie a_session_branding);
# lokal ein gleichnamiges Dev-Projekt auf der OrbStack-Instanz.
# Zwei Keys (Konzept A2), beide server-only — NIE committen!
# Runtime-Key: sessions.write, users.read/write, rows.read/write, health.read
NUXT_APPWRITE_KEY=
# Migrations-Key: databases/tables/columns/indexes read+write — NUR für Migrations-
# Scripts (lokal/CI). Gehört NIE auf den laufenden App-Server (Prod).
NUXT_APPWRITE_MIGRATIONS_KEY=

NUXT_PUBLIC_APPWRITE_ENDPOINT=http://localhost/v1
NUXT_PUBLIC_APPWRITE_PROJECT_ID=branding
NUXT_PUBLIC_APPWRITE_DATABASE_ID=main
# Storage-Bucket für Profilfotos (leer = Avatar-Upload-UI ausgeblendet)
NUXT_PUBLIC_APPWRITE_AVATARS_BUCKET=
# Storage-Bucket für GDPR-Pre-Delete-Snapshots (leer = Löschung ohne Snapshot)
NUXT_PUBLIC_APPWRITE_GDPR_BUCKET=

NUXT_PUBLIC_APP_URL=http://localhost:3006

# hreflang/canonical-Basis für SEO-Alternates (leer = relative Links).
# Prod: https://branding.supply
NUXT_PUBLIC_I18N_BASE_URL=

# E-Mail (leer = Mail-Versand aus; lokal: Mailpit localhost:1025).
# AUF PROD PFLICHT (F44-Lehre): die Beta-Einladung HÄNGT an der
# E-Mail-Verifizierung — ohne SMTP verschwindet sie still, und die Site sieht
# dabei vollkommen gesund aus.
NUXT_SMTP_HOST=
NUXT_SMTP_PORT=587
NUXT_SMTP_USER=
NUXT_SMTP_PASS=
NUXT_SMTP_FROM=

# KI (Georges Entwürfe, P2) — server-only, NIE public.
# Leer = es gibt keinen Generator; das §3e-Streaming antwortet dann nicht mit
# Ersatztext, sondern gar nicht (`devStubGenerator` bleibt ausserhalb des
# .playground aus).
NUXT_AI_KEY=
