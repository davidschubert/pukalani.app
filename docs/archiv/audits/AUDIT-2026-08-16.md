# Audit 2026-08-16 (Runde 5) — die am 2026-08-15 nicht abgedeckten Layer

Der Audit vom 2026-08-15 (AU1–AU3) prüfte posts/comments/onboarding/control/
system/core. Diese Runde fuhr drei read-only audit-worker über die sensibelsten
Rest-Layer. **Arbeitsliste ist ausschließlich OPEN-ITEMS / COMPLETE** — dieses
Dokument ist der Beleg, dass die Fläche geprüft wurde, samt PASS-Listen und den
zwei offenen Fragen für David.

**Gesamtergebnis: solide.** Keine Vertraulichkeits- oder Geld-Korrektheits-
Lücke. Zwei Befunde der AU2-/GDPR-Klasse behoben (Details im COMPLETE-Eintrag
„Audit-Runde 5"), ein Agenten-Fehlalarm entschärft.

## Schnitt A — billing + tickets (control-Instanz)

Behoben: `POST /api/billing/checkout`+`/portal` und `POST /api/tickets/:id/files`
in `WRITE_LIMITED` nachgetragen (Drossel-Lücken).

PASS: Stripe-Webhook (raw-body `constructEvent`, Allowlist, idempotenter Upsert
per `stripeSubscriptionId` + Stale-Guard, Verarbeitungsfehler werfen 500 für
Stripe-Retry, No-op/Mapping-Miss geben 200) · `shouldLiftBillingSuspension` nur
bei `active`, abuse unangetastet · Zahlungswarnung-Scope (account vs. tenant/A6)
korrekt, Idempotenz-rowId im Sweep · Cross-Account-Scoping (jeder Read per
userId/subId, Upsert bewahrt `existing.userId`) · Stripe-Keys AES-verschlüsselt,
Row-Permissions leer, kein Key im Log/Client · GDPR-Contributor billing+tickets
(inkl. Stripe-Kundenlöschung) · Ticket-RBAC (`tickets.manage`, AI-Edit auf
`system.manage` eskaliert) · Ticket-Datei-Sicherheit (Magic-Bytes + 10 MB,
Download permission-gated, safe Content-Disposition) · Query.limit überall.

## Schnitt B — events + courses + media (Pool)

Behoben: RSVP/Score/Cover/Enroll/Lesson-Complete-Drosseln; courses
GDPR-Delete-Swallow strikt gemacht.

PASS: Datentür + kein rohes `.tablesDB` (ESLint-Backstop deckt alle drei) ·
actor:'member' auf allen Inhalts-Schreibwegen (M13/A5), withdraw/cancel bewusst
ohne actor · stripTenantKey · Pool-Unique `uq_tenant_slug` · Upload-Härtung
(Magic-Bytes, Content-Length-Vorabweisung, fileSecurity, kein SSRF) ·
Event-Ticket (Doppel-RSVP/-Ticket per Unique-Index, Kapazität via atomarem
`increment(max)`, paid fail-closed, requirePlanProduct überall) · GDPR-
Contributor events+courses.

**Fehlalarm (kein Fix):** courses' `read(Role.users())` auf veröffentlichten
Kursen — verifiziert defensibel: keine Realtime/Direktzugriffe auf Kurs-Rows,
alles läuft durch die tür-gescopte list/get; `read(users)` ist nur
Publikums-Gate (eingeloggt vs. Gast), bewusst so, damit der Katalog VOR dem
Beitritt sichtbar ist.

## Schnitt C — messages + pages + analytics

Keine Fixes nötig. **Privatnachrichten sind dicht** (die höchste Priorität):
`requireConversation`/`isParticipant` vor jedem Read (404 statt 403), Members
nur server-seitig gesetzt, Blockieren fail-closed + bidirektional, GDPR erfasst
conversations/members/messages/blocks (mit Art.-17(3)-Beweis-Ausnahme),
Moderation liest nur den gemeldeten Ausschnitt nie den Live-Body. pages: kein
Draft-Leak (`status='published'` + C18-Guard), Slug tenant-relativ, CMS-Render
über core-Sanitizer. analytics: site_id/Filter nur aus dem Request-Host, Key
server-seitig, Consent-/Plan-Gate, kein Cross-Tenant.

## Offene Fragen für David (keine Fixe — Entscheidungen)

1. **Medien im U20-Community-Export?** `packages/media` hat keinen
   `community-export`-Contributor (events/courses haben einen) — Medien-Items
   fehlen im Export. Womöglich bewusst (Binär-Gewicht). (Kein per-User-
   GDPR-Contributor ist bei media KORREKT — `media_items` trägt keine userId.)
2. **messages Block per roher userId** (`blocks/index.post.ts`) — die
   Block-Route nimmt eine rohe `userId`, während die Sende-Route bewusst nur
   Handles nimmt (keine ID-Adressbuch-Sonde). Blockieren ist schreibend/
   schützend und verrät nichts (keine Existenz-Probe) — also kein
   Vertraulichkeitsloch, aber eine Asymmetrie: gewollt?
